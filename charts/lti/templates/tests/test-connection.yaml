apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lti.fullname" . }}-test-connection"
  labels:
    {{- include "lti.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lti.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
