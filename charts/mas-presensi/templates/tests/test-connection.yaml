apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mas-presensi.fullname" . }}-test-connection"
  labels:
    {{- include "mas-presensi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mas-presensi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
